# Models Module

This directory contains the Graph Neural Network model implementations.

## Structure

- `gnn.py` - Base GNN model classes
- `layers.py` - Custom GNN layers
- `encoders.py` - Node and edge encoders
- `decoders.py` - Link prediction decoders

## Usage

*Instructions will be added as the code is developed.*
